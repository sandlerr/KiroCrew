# Kiro Crew AutoSDE Rules
#
# Root rules cover backend Python plus cross-stack invariants whose file patterns
# span backend, frontend, and docs. Frontend-only rules live in
# website/AUTOSDE.yaml. Both files are enforced by the Claude and Codex PR
# reviewers.
#
# De-Amazoned from the internal package rule set (internal links removed).
# Blocking rules are backed by real vulnerabilities/incidents in this codebase.

custom-rules:
  - id: feature-map-correctness
    blocking: true
    file-patterns:
      - "docs/feature-map/README.md"
      - "website/src/pages/**"
      - "src/kiro_crew/dashboard/handlers/**"
      - "website/src/App.tsx"
    rule: |
      Keep the feature map semantically aligned with every structural dashboard
      change in the pull request. The mechanical gate proves only that the map
      was touched; this rule judges whether that edit is true.

      - For every dashboard page or backend handler added or deleted in the
        diff, and every `<Route>` registration added or removed in
        `website/src/App.tsx`, require the corresponding map row to be added,
        removed, or updated. A new feature needs a new or updated row; a removed
        feature needs its row removed or updated.
      - Verify every changed map row against the code in the diff. Its user
        navigation path, frontend page, backend handler, and representative
        endpoints must exist and identify the feature's real ownership.
      - Reject unrelated map churn. A whitespace-only, reflow-only, or cosmetic
        map edit beside a real structural change is a token touch made only to
        satisfy `scripts/check_feature_map.py`, not evidence the map was reviewed.
      - The map's net additions, removals, and row edits must match the scope the
        pull request claims. Do not accept rows for unrelated features or omit
        a structural feature because the map file changed somewhere else.

  - id: backend-security-controls
    blocking: true
    file-patterns:
      - "src/kiro_crew/**/*.py"
    rule: |
      Enforce KiroCrew backend security controls, based on real vulnerabilities
      found and fixed in this codebase.

      - Never read sensitive credential paths (~/.aws, ~/.ssh, ~/.gnupg,
        ~/.docker, ~/.kube, ~/.npmrc, ~/.pypirc, ~/.netrc, ~/.git-credentials,
        ~/.kiro/crew/.env and the pre-move ~/.kirocrew/.env). File reads of
        user- or LLM-influenced paths MUST go
        through hooks.py, which enforces is_sensitive_path(); trusted
        fixed-path internal config/package reads are exempt. Never weaken that
        keystone.
      - Never trust LLM output. Scan with redact_exfiltration_urls() AND
        redact_credentials() before posting to Slack, the dashboard, or any
        external surface. redact_credentials() catches raw credential patterns
        (AKIA*, SecretAccessKey=, private-key headers) and base64 variants.
      - Validate all MCP tool inputs via validation.py schemas. Never pass raw
        LLM input to the filesystem, a subprocess, or a database.
      - Authorization must be deny-by-default: reject unless positively
        confirmed. Never write `if x and y and z` guards where a falsy value
        silently skips the check (the fail-open bug class this rule guards).
      - All agent subprocesses must go through AcpClient._spawn() (or the
        provider spawn helpers), which apply an OS-level sandbox via wrap_argv().
      - The dashboard binds to 127.0.0.1 by default (local-only). Binding to
        0.0.0.0 is permitted ONLY when token-auth middleware is active. Never
        bind to 0.0.0.0 without token auth — see origin.py:is_local_only() and
        token_auth.py:token_auth_middleware().
      - All tool invocations and permission decisions MUST emit a SEL audit
        event via sel.py.
      - Never hardcode secrets. Store them in ~/.kiro/crew/.env with chmod 600,
        enforced at load time (log a warning if the chmod fails).
      - Auto-approve / YOLO mode: an AD-HOC activation (Slack command, dashboard
        picker, API) MUST auto-expire — either on a timed TTL capped at the
        SafetyOverride 24h ceiling, or, when the operator selects the
        `until_shutdown` duration, when the process stops. Never persist an
        ad-hoc grant across a restart. A grant the operator DECLARED in
        owner-only config (`agent.dangerously_skip_permissions`; the camelCase and legacy `yolo` spellings are also read) MAY
        persist without expiry and IS re-established at each startup: it is a
        standing instruction, is held in memory only (never written to disk), is
        cleared when another approval mode is selected, and MUST remain deniable
        by the enterprise governance ceiling (the `yolo_duration` scope's
        `permanent` and `until_shutdown` members, evaluated fail-closed against
        the HOST profile). There is deliberately NO dashboard toggle for the
        declared grant. Every activation still routes through the SafetyOverride
        singleton (safety_override.py) and emits a SEL event. Never set the mode
        globals directly.
      - Mermaid securityLevel must be 'strict' (iframe sandbox), never 'loose'
        (which allows JS execution from diagram payloads).

  - id: no-blocking-call-on-event-loop
    blocking: true
    file-patterns:
      - "src/kiro_crew/**/*.py"
    rule: |
      Do NOT add a blocking syscall that runs on the asyncio event loop. The
      gateway runs everything on one loop; a single blocking call there freezes
      every task — the user's chat turn AND the liveness heartbeat — until the
      watchdog kills the process and the supervisor respawns into the same
      condition (a crash loop). This has caused multiple production wedges.

      Calls that block in the kernel and MUST NOT run on the loop:
      - subprocess.run / call / check_output / check_call / Popen(...).communicate()
      - os.close() on a file descriptor (PTY master, socket) — can block on a
        wedged peer
      - os.waitpid / os.system / os.popen
      - time.sleep (use `await asyncio.sleep`)
      - synchronous network or DNS (requests.*, blocking socket.*, getaddrinfo)
      - large synchronous file IO or filesystem walks

      If such a call is reachable from the event loop (in an `async def`, or in
      a sync helper called transitively from an async handler, a periodic task,
      or a cron tick), offload it:

          await asyncio.get_running_loop().run_in_executor(
              subprocess_executor(), blocking_fn, *args)

      or rewrite it with an async API (asyncio.create_subprocess_exec,
      asyncio.sleep, aiohttp).

      Allowed (do NOT flag):
      - the call is already inside run_in_executor(...) or handed to a thread
      - one-shot CLI / setup / packaging paths that run before the loop starts
      - test code

      When in doubt, offload — a leaked worker thread is survivable; a frozen
      loop is not.

  - id: no-new-work-on-gateway-boot-path
    blocking: true
    file-patterns:
      - "src/kiro_crew/slack/gateway.py"
      - "src/kiro_crew/dashboard/server.py"
      - "src/kiro_crew/dashboard/handlers/**/*.py"
      - "src/kiro_crew/cli.py"
    rule: |
      Do NOT add work to the gateway boot path. Everything between process
      start and the moment the dashboard socket accepts requests runs once, in
      order, on one thread — so every statement added there is paid by every
      user on every launch, and it delays the point at which the app is usable.
      A slow boot also compounds: the longer startup holds the event loop, the
      more likely the loop-stall watchdog kills the process and the supervisor
      respawns into the same startup, turning a latency bug into a crash loop.

      The boot path means, specifically:
      - `GatewayOrchestrator.run()` up to the `KIROCREW_READY` print
      - `start_dashboard()` up to `await _start_site(site, port)`
      - any `_init_*()` or `setup_*_routes()` reached from those
      - `cli.py`'s gateway command before `asyncio.run()`

      Reject a diff that puts any of these on that path:

      1. A new awaited or synchronous step before the socket binds. Adding one
         is a product decision about startup latency, not a detail.
      2. Subsystem construction as a side effect of route registration. A
         `setup_*_routes()` function must only register routes
         (`app.router.add_*`). Building a pipeline, opening a store, or
         constructing a client there drags that subsystem's whole init onto the
         boot path, where nobody looking at the route file will find it.
      3. Work whose cost scales with user data — row counts, corpus size,
         file counts, session history. This is forbidden on the boot path at
         any measured speed, because the measurement was taken on an empty
         profile and the growth is unbounded.
      4. Unconditional maintenance: orphan sweeps, integrity scans, vacuum,
         reindex, or a migration that scans rather than checks a version. Boot
         is not a maintenance window. Work that finds nothing on a healthy
         install still costs every user every launch.
      5. Eager import or instantiation of a subsystem that is optional,
         disabled, or feature-flagged off. Gate the import, not just the
         handler — an import the enabled-check happens after is not gated.
      6. Touching a lazily-initialized accessor from boot code. A `@property`
         documented as lazy-init is a contract; reading it at startup breaks
         that contract silently and moves the cost to a place its author did
         not intend.

      Instead, defer it:
      - a first-use accessor that constructs once and memoizes on the app
      - `asyncio.create_task(...)` for work that may finish after readiness
      - `await asyncio.to_thread(...)` when init genuinely must happen at boot
        and genuinely blocks, so it does not hold the loop
      - let dependent routes return 503 until the subsystem is ready, rather
        than making every launch wait for a subsystem most launches never use

      Allowed: route registration itself, in-memory wiring, O(1) config reads,
      and bounded fail-closed safety checks that must precede serving traffic
      (those still belong off the loop).

      If a step genuinely must run at boot, the PR must say why it cannot be
      deferred and state its worst-case bound on a large profile — not on the
      author's machine.

  - id: no-test-side-effects
    blocking: true
    file-patterns:
      - "test/**/*.py"
      - "tests/**/*.py"
      - "src/kiro_crew/apps/builtins/**/tests/**/*.py"
    rule: |
      A test must have no side effects outside its own tmp dir. Never register a
      real cron job, write into the operator's real ~/.kiro/crew or HOME, start
      or reconfigure a gateway/service, or create files or directories in the
      repo or anywhere else that outlive the run. These shapes have already
      bitten us: a unit test registered a live cron job, test runs accumulated
      stray directories under the developer's home, and an empty file a test
      left at the repository root was committed and shipped to main.

      A CHILD PROCESS INHERITS THE CWD, and pytest's CWD is the repository root.
      A test that spawns one MUST pass `cwd=` pointing inside its own tmp dir,
      or anything the child writes with a relative path lands in the repo. This
      is the shape the paragraph above does not catch by reading the test: no
      line says `touch` or `open(...,"w")`, the write happens in a grandchild
      process, and the test can assert against its tmp dir and still pass while
      the artifact sits at the repo root. So for any test that runs a child:
      - pass `cwd=<a directory under tmp_path>` to any child that MAY CREATE a
        file, and to every helper that spawns one. A read-only query is exempt
        and sometimes has to be: `git check-ignore` run against the checkout is
        answering a question about the checkout, and pointing it at a tmp dir
        would test nothing.
      - scope an assertion about a file the child MIGHT create to where that
        child's CWD actually is, not to where you hope it wrote. An assertion
        over `tmp_path` proves nothing about a child that ran somewhere else,
        and a security test whose payload escapes its own assertion is worse
        than no test, because it reports the guarantee it failed to check.

      THE PROCESS WORKING DIRECTORY IS SHARED STATE. It is per-PROCESS, so under
      xdist one test's `os.chdir` becomes every later test's starting directory on
      that worker. FLAG a bare `os.chdir` in a test; `monkeypatch.chdir` reverts on
      its own. This is not untidiness: a passing test's `tmp_path` is removed at that
      test's teardown, so a test that chdirs into `tmp_path` and does not come back
      leaves the worker in a DELETED directory, and `Path.cwd()` then raises
      FileNotFoundError in every later test that reaches it — including from inside
      production code (`taskrunner.TaskRunner.__init__` does `work_dir or Path.cwd()`).
      Measured instance and its numbers: testing-conventions.md § Rules. The shape to
      recognise in review is that it reads as "the suite is flaky" -- many files, each
      passing in isolation -- rather than as one test missing one line.

      A BACKGROUND WORKER BEATS EVERY FILESYSTEM CLEANUP, and this is the shape
      neither paragraph above catches. A process singleton whose writer is a daemon
      thread binds its directory ONCE, from whatever the first caller resolved. The
      thread then outlives that test and re-creates the directory on its next flush —
      AFTER the test's own tearDown removed it. `sel.py` is the worked case:
      `SecurityEventLog._init_locked` captures `_default_dir()` and `_flush_batch`
      opens with `mkdir(parents=True, exist_ok=True)`, which left one stray directory
      behind on every run of a suite whose tests all cleaned up correctly. FLAG a test
      that hands a PER-TEST directory to a subsystem with a background thread, task or
      timer; the fix is a session-scoped directory that belongs to no individual test,
      never tidier cleanup. Same family: a bare `MagicMock` config reads TRUTHY, so
      patching `KiroCrewConfig.load` with one starts whatever subsystem is gated on
      `cfg.<x>.enabled` and resolves `Path(cfg.local_dir)` to a RELATIVE path in the
      repo; and a sleeper child (`python -c "import time; time.sleep(30)"`) whose
      kill/wait sits on the happy path instead of in a `finally` or `addCleanup`.

      A SECURITY ANCHOR IS NOT A DATA PATH. Some module-level values are built FROM
      the real home precisely so they can refuse access to it — `security`'s
      extract-into-trust-root pattern, `kiro_usage_api`'s credential-store paths.
      Redirecting one to a tmp dir so a test can be isolated makes that test assert
      against a pattern that no longer matches the thing it protects. FLAG it: stub
      the READER, never move the anchor. The mirror-image defect is equally real —
      handing a runtime-resolved path to a home-anchored matcher, which passes only
      while the suite is reading the operator's REAL home and reports a guarantee it
      never checked.

      Know which floor you are standing on — it is NOT the same everywhere:
      - The ROOTDIR `conftest.py` is the HOST-MUTATION FLOOR and applies to both
        testpaths. It pins $XDG_CONFIG_HOME and the launchd paths, blocks
        host service mutation, pins KIROCREW_HOME per test (with
        `config.paths._resolved_home` reset, since resolving the home CREATES it
        and can run the legacy migration), pins the import-time `~/.kiro`
        bindings via `_isolate_shared_kiro_paths`, scrubs the inherited
        shell-preload and exported-function variables `name_grant` refuses on
        (`BASH_ENV`/`ENV`/..., `BASH_FUNC_*`), pins the SEL default dir
        session-wide, redirects `tempfile`'s base with residue reported, and
        fails the run on residue at the repository root.
      - `test/conftest.py` adds ~20 MORE autouse fixtures, and it applies ONLY to
        `test/`. Tests under `src/kiro_crew/apps/builtins/*/tests/` see the
        rootdir conftest plus that app's own
        `tests/conftest.py` where one exists (`auto_improvement`,
        `code_review_sage`, `spec_builder` have one; the other five apps do not).
        So a test there gets the host floor -- which now includes the data home,
        the import-time `~/.kiro` bindings, the SEL dir, the subagent registry,
        the model-download block and the agent-state sidecar -- but NOT the
        suite-specific isolation (the Slack thread-state reset, the model-window
        cache, the platform-context reset, the reasoning-effort globals, the
        OPTIONS registries, …). FLAG a test in those trees that relies on one of
        THOSE without arranging it itself.
      - `~/.kiro` is a SEPARATE isolation axis from the data home: it is
        kiro-cli's own machine-wide home, shared with the real installed agent,
        so `~/.kiro/settings/mcp.json` is the LIVE agent's MCP server list. A new
        module-level `Path.home()` binding in `src/kiro_crew` must be added to
        `_SHARED_KIRO_PATHS` or excluded with a reason —
        `test/test_host_isolation_floor.py` fails otherwise. That ratchet covers the
        IMPORT-TIME shape only: a lazy resolver such as `config.paths.kiro_home()`
        still names the operator's real `~/.kiro`, because the floor pins neither
        `Path.home()` nor `$HOME`, so a test reaching one of those must isolate it
        itself.

      Assert against the tmp dir, not the real one, and clean up anything you
      create. If a test genuinely needs a host-level effect, it must be opt-in
      and named in the corresponding conftest allowlist with a comment saying
      why.

      NEVER use bare `tempfile.mkdtemp()` or `tempfile.TemporaryDirectory()`
      without cleanup. These create directories that survive the test run and
      accumulate across runs, exhausting inodes — and inode exhaustion returns
      ENOSPC to every other process on the machine while 90% of the BYTES are
      still free, so it does not look like a disk problem. Use pytest's
      `tmp_path` fixture, or pair every `mkdtemp()` with
      `self.addCleanup(shutil.rmtree, path, ignore_errors=True)` ON THE LINE
      AFTER IT. An `rmtree` in `tearDown` is NOT equivalent and is the shape that
      actually leaks: `unittest` skips `tearDown` entirely when `setUp` raises,
      so it leaks on every setUp failure — the failing run nobody is watching
      closely. The rule is: if you create it, you must register its destruction
      in the same scope. `ignore_errors=True` also SWALLOWS a cleanup that could
      not finish, so it is not evidence the directory is gone.

      NEVER use relative `Path("src/...")` or bare string paths to read source
      files from a test. Under pytest-xdist, workers may change CWD, making
      relative paths non-deterministic. Always anchor to the repo root via
      `_REPO_ROOT = Path(__file__).resolve().parents[1]` (for test/ files) or
      the equivalent number of parent hops for deeper test directories, then
      use `_REPO_ROOT / "src/kiro_crew/..."`. This pattern is already
      established in test_builtin_app_assets.py, test_platform_compat.py, and
      test_skill_home_paths.py.

  - id: top-level-imports
    blocking: false
    file-patterns:
      - "src/kiro_crew/**/*.py"
    rule: |
      All imports must be at the top of the file, not inside functions or
      methods. In-method imports make dependencies hard to trace, break IDE
      navigation, hide circular-import issues until runtime, and can make test
      mock patches target the wrong module namespace.

      Exceptions:
      - TYPE_CHECKING blocks: `if TYPE_CHECKING: from ...` (zero runtime cost).
      - Genuine circular-import avoidance — add a `# circular import` comment
        explaining why.
      - Optional dependencies: `try: import foo except ImportError: foo = None`

  - id: harness-parity
    blocking: true
    file-patterns:
      - "src/kiro_crew/acp/**/*.py"
      - "src/kiro_crew/providers/**/*.py"
      - "src/kiro_crew/platform/**/*.py"
      - "src/kiro_crew/config/loader.py"
      - "src/kiro_crew/session.py"
      - "src/kiro_crew/subagent.py"
      - "src/kiro_crew/sandbox.py"
    rule: |
      Kiro Crew drives ONE first-class agent harness — kiro-cli
      (ACP_BACKEND_KIRO, spelled "") — and ADAPTS the others (the dormant
      ACP_BACKEND_CLAUDE seam, KAS, and any bring-your-own harness). An added
      harness may only adapt itself to the seams the Kiro harness already runs
      through. It may not move, widen, generalize, or add a branch to them. The
      invariant ids below are defined in
      docs/system-specs/modules/harness-parity.md; cite the id in the finding.

      The mechanical shapes are already gated on added lines by
      scripts/check_harness_parity.py, so do NOT re-report them. Report only
      what a regex cannot see:

      - H13 — additive at the seam. Registering a harness must be a v1 addition
        at platform/interfaces.py:ProviderRegistry with no CONTRACT_VERSION
        bump. FLAG a change that puts a new conditional, a new required
        argument, a new awaited step, or a new failure mode on the KIRO
        construction path in service of an adapter — including one that reads
        as harmless refactoring (hoisting a kiro-specific step into a shared
        helper, replacing an explicit kiro branch with a table lookup, or
        making a kiro-only default parameter mandatory). The test is not "does
        it still work for kiro", it is "did the kiro path change at all".
      - H14 — the ABC is the contract. A capability the session layer reads off
        a provider must be declared on providers/base.py:LLMProvider with a
        safe default. FLAG a change that adds a hasattr/getattr probe on the
        kiro path to accommodate an adapter, or that leaves a new capability
        reachable only on AcpProvider so a missing attribute silently reads as
        False for the adapter.
      - H9/H10 — no lowest-common-denominator. FLAG a change that collapses a
        per-harness literal (spawn argv construction, PROTOCOL_VERSION, client
        capabilities, the pre-spawn agent materialization, the --model pin) into
        one form every harness accepts. Downgrading the kiro session to make an
        adapter fit is the defect, even when every existing test still passes.
      - H1/H3 — Kiro is the floor. FLAG a change that makes the kiro harness
        conditional on an edition, a feature flag, an env var, or a registry
        lookup, or that makes an unusable persisted acp_backend raise instead of
        degrading to kiro with a logged reason.
      - H6/H7 — capability grants. A new capability predicate must be membership
        in a named ACP_BACKENDS_* set in acp/types.py, and every existing
        harness's membership must be an explicit decision in the diff. FLAG a
        new predicate that grants a capability to a harness the PR never
        mentions. For is_kiro_cli specifically the failure is OPEN — it makes
        sandbox.wrap_argv SKIP Kiro Crew's own seatbelt in favour of the
        harness's internal one — so treat a grant to any non-kiro harness as a
        security finding.

      Allowed (do NOT flag):
      - The dormant ACP_BACKEND_CLAUDE / _is_claude seam and its existing
        negative call sites. They predate this rule, the whole-tree backlog is a
        non-failing report by design, and converting them is a separate change.
      - A pre-existing negative test on a line the PR merely moved or reindented.
      - An adapter-only module (acp/kas_*.py and equivalents) that is entirely
        additive and reachable only when its harness is selected.
      - Widening the acp_backend config enum to admit a preview harness: the
        enum is the value-SURVIVAL domain and selectability is gated separately
        (H4). Omitting it there is the bug, not including it.

  - id: memory-store-seam
    blocking: true
    file-patterns:
      - "src/kiro_crew/context.py"
      - "src/kiro_crew/memory.py"
      - "src/kiro_crew/vector_memory.py"
      - "src/kiro_crew/suggestions.py"
      - "src/kiro_crew/tips.py"
      - "src/kiro_crew/task_executor.py"
      - "src/kiro_crew/task_planner.py"
      - "src/kiro_crew/history_consolidation.py"
      - "src/kiro_crew/messaging/**/*.py"
      - "src/kiro_crew/subagent_manager/**/*.py"
      - "src/kiro_crew/slack/**/*.py"
      - "src/kiro_crew/telegram/**/*.py"
      - "src/kiro_crew/discord/**/*.py"
      - "src/kiro_crew/dashboard/chat_runner.py"
      - "src/kiro_crew/dashboard/handlers/**/*.py"
      - "src/kiro_crew/eval/runner.py"
      - "src/kiro_crew/config/loader.py"
      - "scripts/check_memory_store_seam.py"
    rule: |
      A named memory store is a SILO: cfg.agents[<crew>].memory_store
      (config/loader.py, the `memory_store` field, default "default") selects a
      SEPARATE SQLITE FILE. A new store starts EMPTY by default. Content is never
      copied or inferred automatically; explicit owner-selected copies follow the
      exception below. So a memory read that does not name its store reads
      the operator's own `default` store for a crew that configured otherwise,
      and NOTHING GOES RED: the prompt is well-formed, the store exists, and the
      content is plausible. The reader is the only detector.

      The mechanical shapes are already gated by
      scripts/check_memory_store_seam.py, on the lines a change touches — added
      lines, plus the anchor of a deletion-only hunk, since REMOVING a
      `memory_store=` keyword adds no line at all. The shapes it owns: a missing
      `memory_store=` on the enclosing call, a store derived from `agent=`, a bare
      string literal, a literal `memory_store=None` (omission spelled out, since
      the consumer collapses None onto its v1 workspace arm), an unanchored reference
      to the target methods, and `get_memory_for` / `get_lessons_for` called with
      nothing or a literal None. Do NOT re-report those. Report only what an AST
      walk cannot see:

      - A NEW READ PATH the gate does not know about. The gate matches four
        names. FLAG a change that reaches memory some other way — constructing a
        MemoryStore or a vector index directly, adding a second context builder
        entry point, or adding a resolver alongside get_memory_for — without
        threading the store name through it. A read path the gate cannot see is
        exactly the one that will be added without a store.
      - THE WRONG NAMESPACE passed as the store. The silo key is the named store
        and nothing else. FLAG a call that passes a workspace name, a member or
        channel slug, a project directory, a session key, or a kiro agent name
        into `memory_store=`. These read as plausible because several of them are
        in scope at every call site, and the gate cannot tell a store name from
        any other string.
      - RESOLUTION DUPLICATED AT THE CALL SITE. The store is resolved once, by
        config.loader.resolve_agent_bindings, and its result is passed by name.
        FLAG a call site that indexes cfg.agents itself, re-implements the
        fallback to config.default_memory_store, or resolves a store from an
        agent name inline — the measured failure mode is a silent `default` with
        requested_resolved=False, for exactly the crew that configured otherwise.
      - THE TWO ROOTS CONFLATED. The markdown/FTS root and the vector file are
        different paths and a per-store resolver has to reproduce both for the
        `default` store: the markdown root is memory.workspace_dir(), which is
        config_dir()/"workspace" (memory.py), and the vector file is
        config_dir()/"memory.db" (vector_memory.py's `db_path or ...` default).
        FLAG a change that returns config_dir() itself as a store's markdown
        root, or that moves the default store's vector file: either one takes
        every existing user's preferences.md, projects.md and history/ out from
        under the consolidator and `kirocrew memory search`, with no error.
      - IMPLICIT CONTENT TRANSFER INTO A NEW STORE. FLAG automatic copies, seeds
        or backfills, and any fallback read of Global Memory or another store when
        the selected store is missing or unreadable. Explicit owner-selected
        copies must satisfy the allowed case below.
      - THE GATE ITSELF WIDENED. FLAG an added ALLOWLIST entry (the table is
        intended empty and its size is printed on every run), a rule made
        conditional, or a `# memory-store-ok:` marker whose reason does not
        survive reading — the reason exists so a reviewer can decide whether the
        call really has no store to name.

      Allowed (do NOT flag):
      - An explicit owner-authorized copy that names both source and destination,
        copies only user-selected records, records source provenance, preserves
        existing destination records, and leaves ownership and store bindings
        unchanged. This does not authorize automatic inheritance, fallback reads
        or shared private stores.
      - The pre-existing unconverted call sites. The whole-tree backlog is a
        non-failing report by design, pinned per rule by
        test/test_memory_store_seam.py, and converting them is separate work.
      - A call site that names the store and ALSO passes `agent=`. The two are
        independent: `agent=` selects the kiro-cli mode, `memory_store=` selects
        the silo, and the store being explicit is the point.
      - `memory_store=cfg.default_memory_store` or
        `memory_store=bindings.memory_store_name`. Reading the default store on
        purpose is a legitimate answer; naming it is what makes it reviewable.
      - `ContextBuilder.get_memory_for(ws_name)` / `get_lessons_for(ws_name)`
        holding a WORKSPACE name. That parameter is `workspace`, resolved through
        config.loader.workspace_dir_for. Both resolvers now also take a separate
        `memory_store=` keyword, which wins when it names a non-default store.
        A store name handed to the POSITIONAL workspace argument is still the
        wrong-namespace bug (an unmapped workspace falls back to
        config_dir()/"workspace", the default markdown root).
      - Diff scoping is the standing enforcement mode. The whole-tree report
        retains the operator heartbeat and evaluation coordinator calls, whose
        unscoped spelling represents their Global context. Their exact backlog
        is pinned by test/test_memory_store_seam.py; new unscoped call sites
        remain forbidden. Whole-tree enforcement is not a promised rollout step.

  - id: changelog-is-written-at-version-bump-only
    blocking: true
    file-patterns:
      - "CHANGELOG.md"
    rule: |
      CHANGELOG.md is written ONLY when a version is bumped, and released
      history in it is immutable. The spec for the section itself is
      docs/build/changelog.md.

      This rule exists because the file has already been destroyed once: a
      docs commit on a release branch replaced CHANGELOG.md instead of
      prepending to it, deleting the entire [0.2.0], [0.1.3] and [0.1.2]
      sections (322 lines) and leaving one machine-generated section behind.
      Nothing failed, no test covered it, and the loss was noticed only when a
      user asked why the dashboard's Releases page had gone empty. A changelog
      is append-only shipped history; a deletion in it is unrecoverable from
      the artifact a user has installed.

      - FLAG any change to CHANGELOG.md in a PR that is not a version bump or a
        release cut. A feature, fix, refactor, test, or docs PR MUST NOT touch
        this file — the release PR writes the section for everything that
        shipped. If a PR author wants their change remembered, the commit
        subject is the record.
      - FLAG any diff that DELETES or MODIFIES a line inside an existing
        released `## [X.Y.Z]` section. A release PR adds exactly one new
        section at the top and leaves every pre-existing section
        byte-identical. Treat a removed `## [` heading as a blocking finding on
        its own, whatever the stated reason (merge-conflict resolution,
        regeneration, "cleanup", branch divergence).
      - FLAG a new `## [Unreleased]` section. There is no Unreleased section:
        unshipped changes live in the commit log until a bump names them. Use
        `git log --oneline <last-tag>..HEAD` to see what is pending.
        `scripts/check_changelog_history.py` also refuses this mechanically, so
        spend your attention on the section's CONTENT rather than re-deriving
        this; report it only if the gate somehow did not.
      - FLAG a section assembled from a commit dump — a bare list of commit
        subjects, a "Bug Fixes (N total)" count, or a trailing "and N more
        (see commit log)". That is not a changelog entry; it is the thing a
        changelog entry exists to replace, and nobody reads it. A curated
        `### Notable fixes` list of individually-named user-observable outcomes
        is NOT a dump and is allowed — see docs/build/changelog.md for the line
        between them.
      - FLAG a heading that is not a plain release: `## [0.3.0-insider.9]`,
        `## [0.3.0-rc.2]` and similar prerelease spellings must not appear.
        A prerelease is a draft of its base version and is folded onto it by
        src/kiro_crew/changelog.py; giving it its own heading puts a build
        stamp in the reader's version archive. Mechanically refused by
        `scripts/check_changelog_history.py` as well; a release branch writes
        its section once, under the release's final heading.

      Allowed (do NOT flag):
      - A release PR that prepends one new `## [X.Y.Z] - YYYY-MM-DD` section
        and, in the same commit, bumps src/kiro_crew/__init__.py.
      - Correcting a genuine factual error inside an already-shipped section,
        when the PR body says that is what it is doing. Say it out loud; an
        unexplained edit to shipped history is the defect this rule exists for.
      - Porting an already-written section verbatim between main and a release
        branch, where the diff adds a section and removes nothing.

  - id: recorded-decisions-are-not-regressions
    blocking: true
    file-patterns:
      - "docs/decisions/**"
      - "website/src/**"
      - "src/kiro_crew/**"
    rule: |
      docs/decisions/ is the team's record of product and design decisions a
      person made: one immutable file per decision, indexed by its README. The
      rules are in docs/decisions/README.md. This rule exists because a
      recorded decision was reversed by a pull request that read as an ordinary
      fix: a single nightly friction row asked for a longer button label, the
      PR changed it, and nothing in review knew the short label had been chosen
      on purpose. A decision that has to be re-made every time a report
      disagrees with it was never recorded at all.

      - FLAG any diff that EDITS, RENAMES or DELETES an existing file under
        docs/decisions/ other than README.md. An entry is written once. A
        change of mind is a NEW entry carrying a `Supersedes:` line that names
        the old one, and the old file stays byte-identical.
        `scripts/check_decisions_history.py` refuses this mechanically in Fast
        Gate, so spend your attention on the next three points; report it only
        if the gate somehow did not.
      - FLAG a change to a user-facing label, wording, placement or behaviour
        that an entry under docs/decisions/ records, when the PR adds no
        superseding entry. Grep the directory for the control, key or
        behaviour the diff touches. That the change closes an issue, answers a
        friction report, satisfies a review finding or makes a test read
        better is not a reason: an entry outranks all of those. A report that
        collides with an entry is a feature request against a settled
        decision, linked to the entry by its file path and left to a
        maintainer; no fix is made from the report itself. Treat the reversal
        as a regression, whatever the PR title says.
      - FLAG a PR that carries an entry with a `Supersedes:` line, with or
        without the code change it authorizes. This flag is by design: the one
        way a decision changes is a dedicated PR carrying the superseding
        entry, and a maintainer's override of this flag is what lets it land.
        The entry is not a license to un-flag the code change beside it; the
        maintainer's override is the approval, and it covers both.
      - FLAG a new entry, superseding or not, whose Evidence links no
        on-record artifact carrying the human decision it records — an issue,
        a pull-request comment or a commit, checkable by anyone. "Decided in
        chat" with no link is not evidence; the person restates the decision
        on the PR and the entry cites that comment. An entry that decides on
        the agent's own authority is refused.

      Allowed (do NOT flag):
      - Adding a new entry that names the person who decided and links the
        evidence, with the matching index row in docs/decisions/README.md.
      - Adding a founding entry (one with no `Supersedes:` line) that records
        the outcome a person already decided AND links that person's on-record
        statement of it, in the SAME PR as a change that RESTORES that outcome
        after an unrecorded reversal. Nothing is being changed; the ledger is
        catching up with what was decided, and the link is what keeps this
        path from being a way to write a decision nobody made.
      - Editing docs/decisions/README.md itself (index rows, rule wording).
      - A change to a label, placement or behaviour that no entry records.

  - id: recurring-defect-patterns
    blocking: false
    file-patterns:
      - "src/kiro_crew/**/*.py"
    rule: |
      Defect patterns harvested from this repository's own fix history (each
      appeared as a SERIES of shipped bugs, not a one-off). Flag a changed
      line that re-introduces one of these classes as an advisory FINDING:

      - A user-supplied or config-sourced STRING used directly in a boolean
        context, especially for a security-relevant toggle: `bool("false")`
        is True, so `if config.get("dangerously_skip_permissions"):` passes
        for the string "false". Require an explicit typed comparison or a
        real bool before the branch.
      - A docstring or comment that CONTRADICTS the code below it (an
        allowlist entry the docstring names but the code omits, a comment
        saying "denies by default" above a loader that permits). Either the
        prose or the code is wrong; both shipped as bugs here. Flag the
        contradiction itself — do not guess which side is intended.
      - A documented allowlist/denylist whose entries have no round-trip
        assertion: when a diff adds or edits such a list alongside prose
        (docstring, spec, comment) enumerating its members, expect a test
        asserting each documented member is actually in the list.

      This rule is maintained by the pattern-harvest loop (fix PRs propose
      candidates in their "Pattern harvest" section; accepted ones land
      here or in semgrep/). Sentinel-return misuse (str.find/rfind -1 in a
      boolean context) is deliberately NOT listed: semgrep owns it
      (semgrep/find-sentinel-truthiness.yaml).

  - id: no-new-builtin-apps
    blocking: true
    file-patterns:
      - "src/kiro_crew/apps/builtins/**"
    rule: |
      The set of built-in apps is CLOSED. A new app ships as an EXTERNAL app
      published through the KiroCrewApps registry (what the App Store's
      Discover page lists), never as a new built-in in this repo. A built-in
      rides the wheel: it grows every user's install footprint, cannot be
      uninstalled (only disabled), and couples the app's release and fix
      cadence to Kiro Crew releases — the registry exists precisely so an app
      can be listed, updated, and corrected with a catalog publish instead of
      a client ship.

      - FLAG any PR that adds a new app directory under
        src/kiro_crew/apps/builtins/ (a new app.json). The publishing path is
        the KiroCrewApps registry; point the author there. An explicit
        maintainer decision recorded on the PR (the /ai-review override flow)
        is the only exception — the default is closed.
      - FLAG restoring a withdrawn built-in (the Board app) or un-hiding the
        Channels app from the App Store (flipping "hidden": true in
        src/kiro_crew/apps/builtins/channels/app.json). Both are deliberate
        fork divergences an upstream sync must not undo.

      Allowed (do NOT flag):
      - Changes inside an EXISTING built-in app directory: fixes, features,
        tests, i18n, refactors.
      - Deleting an existing built-in, or moving one OUT of the repo to the
        registry — that is the direction this rule pushes.

  - id: no-sync-store-call-from-a-coroutine
    blocking: true
    file-patterns:
      - "src/kiro_crew/**/*.py"
    rule: |
      A coroutine on the gateway event loop MUST NOT call a synchronous
      persistent-store method. This is narrower and stricter than
      `no-blocking-call-on-event-loop`: those are kernel-blocking syscalls, and a
      store call looks harmless because it usually returns in under a
      millisecond. It stops being harmless the moment a SECOND connection exists
      — a `kirocrew doctor` reader, a second gateway on the same home, a
      `sqlite3` shell, or `journal_mode=DELETE` on a network home — because then
      `PRAGMA busy_timeout` can hold the loop for its full window per call.

      A durable store owns a single connection behind a single lock and a
      dedicated writer thread. Route the DATABASE phase through that writer
      (`await store.run(...)`, or the surface's existing `*_async` seam) and keep
      Futures, Events, slot counts, wake callbacks and in-memory run state ON the
      loop. Do not hand a whole coordinator that touches asyncio primitives to a
      thread: split the site explicitly into a DB phase and a loop-apply phase.

      Judge by CALL CHAIN, not by the enclosing function. A sync helper, a sync
      closure, or a facade method invoked from an `async def` is on the loop.
      `check_sync_io_in_async` being clean means nothing here: it is intraprocedural.

      THE TRAP THAT COST THIS REPO THREE REVIEW ROUNDS: a caller wrapped in
      `try/except Exception` SWALLOWS the on-loop guard's exception, so the site
      is invisible to an armed run and the suite goes green over it. When a
      caller swallows, pin the site on the store's on-loop COUNTER
      (`loop_thread_calls` before/after) instead, or narrow the `except` to the
      exceptions it actually means.

      FLAG a new store call reachable from a coroutine. FLAG an `except
      Exception` newly wrapped around one, which hides it.

      Allowed (do NOT flag):
      - the DB phase already goes through the writer or an `*_async` seam;
      - a terminal write on an `except CancelledError` / `except BaseException`
        unwinding arm, where an `await` can be interrupted before the write is
        submitted and a dropped terminal write leaks state to the next boot —
        but it must say so in a comment;
      - a call whose RETURN VALUE the caller cannot proceed without and which no
        seam can supply — again, only with the reason stated;
      - a site provably unreachable with a live store (constructed and called in
        one statement block with no await between), stated as such;
      - CLI / one-shot paths that run with no loop, and test code.

  - id: persist-before-you-publish
    blocking: true
    file-patterns:
      - "src/kiro_crew/**/*.py"
    rule: |
      When a durable record is the authority for a piece of state, the PERSISTED
      transition must be a precondition for publishing that state in memory —
      not a task fired alongside it.

      Real defects this rule exists for, both found in review here:
      - a run marked running in memory while its `RUNNING` write was merely
        SCHEDULED, so the next transition was refused by the state machine and
        the durable wait reason was lost;
      - a resume that restored a slot count, set its resume event and returned
        success after the durable wake had RAISED, leaving the row and the
        runtime disagreeing about whether the work was allowed to continue.

      Require, for a state a record owns:
      - the write is AWAITED and its RESULT inspected before the in-memory
        publish, the counter change, or the event set;
      - a refused transition or a store failure leaves an explicitly retryable
        or explicitly terminal state — never log-and-report-success;
      - where a generation or lease fences the row, the value published is the
        one the write returned, not one read before it;
      - reserve-then-commit for capacity: take the reservation on the loop, do
        the write, then publish — and release the reservation on EVERY refusal.

      A second fire-and-forget write is not an ordering guarantee. Two posted
      writes are ordered only if they share one writer and one submission order;
      one posted and one inline are not ordered at all.

      FLAG a `create_task` / fire-and-forget durable write whose in-memory effect
      is applied before it lands. FLAG a boolean success returned on a path where
      the write raised or returned falsy.

  - id: a-ratchet-may-only-tighten
    blocking: true
    file-patterns:
      - "test/**/*.py"
      - "website/src/test/**"
      - "website/src/**/*.test.ts"
      - "website/src/**/*.test.tsx"
    rule: |
      A test that pins a COUNT, an exact set, or a universal property over every
      site is a ratchet: it exists so that adding an unreviewed instance of some
      class fails. Re-pinning it because the diff added a legitimate instance is
      correct. LOOSENING it so the diff fits is not, and it is the most
      comfortable way to ship the defect the ratchet was written to catch.

      FLAG, in a test the diff touches:
      - a universal assertion (`for site in sites: assert ok(site)`) replaced by
        an allowlist or an expected-failure set — especially when the entries are
        this PR's own new code;
      - a strict count or set equality relaxed to `>=`, `<=`, `in`, `issubset`,
        or a truthiness check;
      - a poll count, timeout or retry budget RAISED so a race stops firing. A
        flake is a missing signal: wait on the event, the store row or the future
        that the production path actually resolves, and bound it only as a
        lost-run ceiling;
      - a newly added `skip` / `xfail`, a broadened `except`, or a deleted
        assertion in a file the diff otherwise only extends.

      Allowed (do NOT flag): the count MOVES to the new true value and the
      comment naming the roles moves with it; a slice/region marker is widened so
      an extracted fragment still executes with every name it references defined;
      the assertion is genuinely re-derived and stated to be stronger.

      When a ratchet cannot be satisfied without loosening it, that is the
      finding — say so on the PR instead of editing the ratchet.

  - id: a-sandbox-widening-is-not-incidental-scope
    blocking: true
    file-patterns:
      - "src/kiro_crew/sandbox.py"
      - "src/kiro_crew/security.py"
      - "src/kiro_crew/hooks.py"
    rule: |
      What the OS sandbox seals, masks, no-follow pins or refuses is an
      OPERATOR-FACING contract: it decides which of their own files their agent can
      still reach. A widening nobody asked for does not surface as a failed review;
      it surfaces days later as work that stopped, reported as a pattern instead of
      a cause, with the legitimate operation gone and no record that anyone chose
      to lose it. So the scope is the operator's to widen. It is not a reviewer's
      to request, and not an author's to add on the way past something else.

      FLAG, in a diff that touches these files:
      - a new entry on a seal / mask / no-follow / precreate list, or a new
        refusal, whose path the PR's stated intent does not name. A leaf added
        because it looked like a sibling of one that IS named is exactly the
        unrequested widening.
      - an existing fence tightened past the threat the PR names — a predicate
        widened to a superset, a per-value inspection replaced by a blanket deny,
        a check moved onto a path the PR does not otherwise touch.
      - hardening added in round N only to answer a reviewer's "spelling Y reaches
        it too". A review comment is not the operator's authorization, and this
        converges on nothing: #12103 spent ELEVEN rounds on one span that way,
        each round a new named variable. Cite the seal that actually fails on a
        path the PR already changes, or post a rebutted disposition and keep the
        code.
      - a widening whose whole justification is defense in depth with no reachable
        input named. "Could be reached" is not a threat; a caller is.

      An added invariant ships with the data it demands, in the SAME commit, and
      at import time the blast radius is the whole PACKAGE rather than one test.
      #11556 added a module-level `assert` over the full sealed union while writing
      wording for two of five leaves. `import kiro_crew.sandbox` then raised on
      main, so every backend test in the repo failed at COLLECTION — including
      `test_every_strict_leaf_of_either_shape_is_covered_by_the_delegated_guard`,
      the test written to pin that exact invariant, which could not report the gap
      because it could not be collected either. An import-time self-check silences
      its own witness. FLAG an invariant whose inputs the diff leaves incomplete,
      and say which data is missing.

      Allowed (do NOT flag): a widening the PR's stated intent names, carrying the
      reachable input and the consequence in writing; a completion that brings an
      EXISTING invariant's data up to what it already demands; any narrowing.

  - id: prefer-subtracting-a-duplicate-mechanism
    blocking: false
    file-patterns:
      - "src/kiro_crew/**/*.py"
    rule: |
      A long-lived branch can be in review while `main` lands its OWN fix for the
      same incident. Merging both leaves two mechanisms deciding one question,
      and the weaker one usually wins by being consulted last.

      This happened here: the branch carried a self-report side channel so the
      supervisor would not kill a busy daemon, while `main` landed an escalated
      probe for the same incident. Kept together, a genuinely wedged daemon's
      worst-case detection went from 168 s to 840 s, and the side channel's own
      signal was a flag that stays true until the socket is closed — so it
      vouched for a daemon whose accept path was already dead.

      For a mechanism this PR adds that answers a question something else already
      answers, ask in order:
      - does `main` (or another merged PR) already handle this? Compare the
        rebase's incoming hunks against what the branch built;
      - if both exist, which signal is FRESHER and which can actually observe the
        failure? Prefer the one on the path that has to work anyway;
      - what does keeping both do to the worst case? Grace multiplied by grace is
        the usual answer, and it is rarely intended;
      - can the added one be SUBTRACTED, leaving the upstream fix? Prefer that
        over reconciling two mechanisms.

      FLAG a second retry ladder, liveness oracle, cap actuator, admission gate
      or capacity accounting where one already exists, and say which one to keep.
      Do not flag a genuine second OPINION that is deliberately independent and
      documented as such.

  - id: a-refusal-is-not-a-commit
    blocking: true
    file-patterns:
      - "src/kiro_crew/**/*.py"
    rule: |
      The inverse of `persist-before-you-publish`, and it shipped separately.
      That rule says do not publish AHEAD of the write. This one says: when the
      write comes back REFUSED, do not carry on down the committed path — and
      when a refusal has more than one CAUSE, do not treat the causes alike.

      A refused durable write has two families of cause and they need OPPOSITE
      handling:
      - the state machine refused it and the row is still THIS owner's at THIS
        generation. Nobody else will ever report the outcome, so the caller's
        own compensation (the parent wake, the slot release, the retry) is the
        only thing that will happen. It MUST run.
      - a generation or lease fenced it: the row belongs to another owner now,
        whose own report will run the same compensation. Running it here is not
        redundant, it is WRONG — it reports an outcome for work that is still in
        flight.

      Found in review here: a child's terminal write fenced by its replacement's
      generation still ran the parent propagation, and the ledger counts a child
      terminal from the ID it is handed, so the parent left `waiting_children`
      for a claimable state while the replacement child was still `running` —
      unbounded silent loss, with no reconciliation until the next boot sweep.

      Require, at a site that inspects a durable write's result:
      - the two causes are DISTINGUISHED where they call for different handling,
        by re-reading the row (state AND generation), never by the boolean alone;
      - "another owner's" is proved, not assumed: a store that could not be READ
        is not another owner, because an outage leaves this owner's generation on
        the row, so it falls through to the compensating path;
      - a fenced-but-TERMINAL row still gets the compensation. Another owner
        already settled it, the compensation is idempotent there, and skipping it
        strands whoever was waiting when the terminal came from a path with no
        propagation of its own (an operator cancel, a boot sweep).

      FLAG a compensating action (a parent wake, a tree cancel, a slot release, a
      counter decrement) that runs unconditionally after a write whose result was
      consumed only by a log line. FLAG "skip on a stale generation" written as a
      test on the generation alone, with no terminality check.

  - id: an-invariant-line-needs-a-mutation-pin
    blocking: false
    file-patterns:
      - "src/kiro_crew/**/*.py"
      - "test/**/*.py"
    rule: |
      A line that CARRIES an invariant — a `generation=` argument, an
      `only_from=` predicate, a cap comparison, an ordering guard, a `frozenset`
      membership test — is load-bearing exactly to the extent that deleting it
      reds a test. Most of them here red nothing.

      Measured on this PR: deleting `generation=record.generation` from the task
      panel's `answer_input` call reds ZERO of 225 tests, and so does deleting it
      from the `cancel_wait` call beside it. Both are the only thing standing
      between an operator's answer and a DIFFERENT question the row asked after a
      re-dispatch, and the state fences around them are satisfied in exactly that
      case.

      Half-pinned is the shape that ships. A fence pinned only on the REFUSED
      side goes green when it is LOOSENED, because the loosening produces the
      commit the healthy-side test never asserts.

      For an invariant line the diff adds or edits, expect a test that:
      - reds when the line is DELETED, and
      - reds when it is LOOSENED to the next-weakest thing a reader might write
        (a fence's predicate widened to the full claimable set, a generation
        replaced by None, a strict cap turned into a warning), and
      - asserts the HEALTHY side too: under a satisfied fence the write COMMITS
        and the row moves. Otherwise the pin passes on a fence that stopped
        distinguishing anything.

      The check is mechanical: apply the mutation, run the tests that cover the
      file, report the count. Zero red on a load-bearing line is the finding.
      FLAG an invariant line added with no such test, and say which mutation
      survives.

  - id: a-bound-bounds-every-field-it-retains
    blocking: true
    file-patterns:
      - "src/kiro_crew/**/*.py"
    rule: |
      A cap on how many items are stored bounds memory only when every STORED
      FIELD of an item is bounded too. 4096 rows of an unbounded title is
      unbounded, and the cap's own comment will say the growth is refused.

      Found in review here: the native-child roster capped its ROW COUNT at 4096
      and type-checked the id, while each row also retained a backend-authored
      name, title and status with no length bound at all — and a second structure
      keyed on the same population capped itself at 256, so one announced roster
      split into two classes by list position, and the ids past 256 lost their
      approval routing while the count still called them children.

      Require, for a structure retaining externally-controlled data:
      - a bound on the COUNT and a bound on each retained STRING or nested
        container, applied at the point of RETENTION, not at a render site
        downstream — a bound applied where the value is displayed leaves the
        store unbounded;
      - one NAMED constant shared by every structure bounding the same
        population, so the two cannot drift. Two literals bounding one population
        is the defect, whatever the numbers are;
      - the admission answer for the count GATES the other stores: an item the
        count refused must get no row anywhere, or the refused item can never be
        recognised as a duplicate and reintroduces the growth the cap refuses;
      - overflow is COUNTED and said out loud once per snapshot. A truncated tail
        otherwise reads exactly like a population that never named those items,
        and the audit reason for anything denied past the cap must name the
        truncation rather than borrowing the reason for "never announced".

      FLAG a `[:N]` slice or a `len(...) >= CAP` guard whose items carry unbounded
      strings. FLAG two different bounds on the same population.

  - id: an-operator-facing-string-states-what-was-measured
    blocking: false
    file-patterns:
      - "src/kiro_crew/config/sections.py"
      - "config-baseline.json"
      - "docs/system-specs/**/*.md"
      - "src/kiro_crew/**/*.py"
    rule: |
      `recurring-defect-patterns` already flags a docstring that contradicts the
      code BELOW it. This is the case adjacency cannot catch: a config help
      string, a CLI message or a spec sentence that states an arithmetic
      CONSEQUENCE which no gate evaluates and which is false.

      Found in review here: `mcp_gateway.spawn_queue_wait_secs` shipped help
      saying "raising this above 600 needs that constant raised too, or the stub
      gives up before the queue does". Measured, the daemon arms
      `min(asked, key)` less a refusal margin — so the DAEMON gives up first and
      raising the key buys a queued stub no extra wait at all. An operator who
      believed the string raised the key, got nothing, and concluded the queue
      was broken. Three copies of the sentence (schema, committed baseline,
      owning spec) were byte-identically wrong, and the snapshot-parity test held
      all three consistent with each other the whole time.

      Where a user-facing string states a consequence a reader could ACT on
      ("raising this...", "the default matches...", "this is capped by..."),
      require a test that MEASURES the claim through the production code rather
      than re-implementing its arithmetic, and that reads each copy of the
      sentence down to the text about that key — a spec paragraph, not a whole
      file, or the assertion passes on a word borrowed from elsewhere.

      Also assert the FALSIFIED direction in the spellings that shipped it. A
      test that only checks the new wording is present goes green again the day
      someone restores the old sentence beside it.

      FLAG a changed help/spec sentence naming a threshold, a default equality or
      a cap relationship with no test that measures it. Do NOT flag ordinary
      descriptive copy that states no consequence.
